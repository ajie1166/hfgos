var ws = null;
var conArray = ["ws://127.0.0.1:8181"];
var wsConnection = {};
wsConnection.initWS = function () {
    ws = new WebSocket(ConnectionArray[0]);
    ws.onopen = function () {

    };
    ws.onmessage = function () {

    };
    ws.onclose = function () {

    };
};